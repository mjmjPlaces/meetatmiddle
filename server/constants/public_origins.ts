/**
 * 프론트 공개 URL — ODsay WEB 키(Origin/Referer), 문서 예시와 맞춤.
 * 배포 도메인이 바뀌면 여기만 수정하면 됩니다.
 */
export const PRODUCTION_FRONTEND_ORIGIN = "https://samemeet.com";

/** 로컬에서 Express가 index.html을 띄울 때의 Origin (개발 전용) */
export const LOCAL_DEV_FRONTEND_ORIGIN = "http://localhost:4000";
